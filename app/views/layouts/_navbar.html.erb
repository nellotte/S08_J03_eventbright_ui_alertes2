    <!-- NAV BAR EVENTBRIGHT -->
    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">My Eventbright Project</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Accueil", events_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Proposer un évenement", new_event_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Tous les évènements</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Mon compte
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <% if user_signed_in? %>
                  <li><%= link_to "Mon profil", user_path(current_user), class: "dropdown-item" %></li>
                  <li><%= link_to "Editer profil", edit_user_registration_path, class: "dropdown-item" %></li>
                  <li><%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "dropdown-item" %></li>
                  <% if current_user.is_website_administrator? %>
                    <li><%= link_to "Administration des utilisateurs", website_administrator_users_path, class: "dropdown-item" %></li>
                    <li><%= link_to "Administration des évènements", website_administrator_event_submissions_path, class: "dropdown-item" %></li>
                  <% end %>
                <% else %>
                  <li><%= link_to "Connexion", new_user_session_path, class: "dropdown-item" %></li>
                <% end %>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <!-- FIN NAV BAR EVENTBRIGHT -->